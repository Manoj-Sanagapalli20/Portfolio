# Render.com / Railway / Generic Cloud Deployment
# Place this file at project root.
# For Render: set Build Command = npm run build, Start Command = npm start

# This file documents the deployment requirements.
# No secrets are stored here.

# Required environment variables (set in your cloud dashboard):
# PORT  — automatically injected by Render, Railway, Fly.io, Heroku, etc.
#         Our vite.config.js and package.json start script both read this.

# Build Command:  npm run build
# Start Command:  npm start
# Publish Dir:    dist
